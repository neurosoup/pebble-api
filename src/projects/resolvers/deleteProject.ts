import { Context } from '../../server';
import { Mutation, MutationDeleteProjectArgs } from '../../types';

export default async (parent: Mutation, args: MutationDeleteProjectArgs, context: Context) => {
  return null;
};
